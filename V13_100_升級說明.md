# V13.100 升級內容

1. 股票池由60檔擴充為100檔。
2. 新增首頁 DAVID 今日 TOP 10：掃描100檔後依綜合分數自動排序。
3. Page 9 改為「本週熱門候補」：依5日動能、量能、20日突破、六買等自動排序。
4. Page 10 改為「異常轉強股」：依單日爆量、漲幅、MACD翻正、MA5/MA20翻多與六買等自動排序。
5. Page 1~6 為六大核心族群；Page 7~8 為 DAVID 自選20檔。
6. GitHub Actions 每日快照改為100檔，且優先讀 Supabase stock_config，不足100檔自動補新版預設。
7. 保留既有 stock_history；新增 SQL migration 將 stock_config.position 上限由60改為100。
8. requirements 加入 pydantic>=2.7,<3，避免 TypeAdapter 相容性問題。

## 既有 Supabase 必做一次
在 SQL Editor 執行 `supabase_upgrade_v13_100.sql`。
執行後回到網頁，按「↩️ 還原100檔」即可把新版100檔設定寫入雲端。

## 安全提醒
不要把 Supabase Secret / service_role key 上傳或提交到 GitHub。
